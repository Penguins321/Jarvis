<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.prictice.demos.mapper.LearnMapper">
    <insert id="insert">
        insert into student(name,home) values (#{name},#{home})
    </insert>


    <select id="getListData" resultType="java.lang.String">
        select user_name from  dc_uc_user limit 50;
    </select>
</mapper>
